ls html -l #1. логирую все файлов в папке html
cd html # перехожу в папку html
ls -l # просмотриваю файлы в папке html
grep 404 acces # логирую все совпадения с шаблоном 404 в файле 
cp 404.html 403.html # создаю копию 404.html с названием 403.html
sed -i 's/404/403/g' 403.html # при помощи команды sed заменяю регуляркой 404 на 403 в в файле 403.html
chmod 640 index.html # меняю права доступа к файлу для пользователя, группы и всех остальных
ln -s acces.log AccesLog # создаю символическую в current директории для acces.log
ls -a -l /var/ # логирую все файлы из var в удобночитаемом формате
cat>503.html # создаю файл 503.html с нижеприведённым текстом
<html>
    <head>
        <title>503</title>
    </head>
</html>
